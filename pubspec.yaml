name: flutter_number_flow
description: Animated Flutter widget for smooth number transitions. Perfect for counters, currency displays, and statistics with locale support.
version: 0.1.0
homepage: https://github.com/Piyushhhhh/number-flow-flutter
repository: https://github.com/Piyushhhhh/number-flow-flutter
issue_tracker: https://github.com/Piyushhhhh/number-flow-flutter/issues

topics:
  - animation
  - counter
  - number
  - formatting
  - widgets

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: